//Let's get the needed classes and files for initializing
const config = require("../../config/config");

const Commands = require("./Commands");

//Let's Roll!


/*
    Let's initialize the terminal's interface
*/

